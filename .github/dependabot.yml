version: 2
updates:
  # ── npm: backend ─────────────────────────────────────────────────────────
  - package-ecosystem: "npm"
    directory: "/backend"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5
    groups:
      backend-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      # Major bumps for the Stellar SDK require manual review & contract re-testing
      - dependency-name: "@stellar/stellar-sdk"
        update-types: ["version-update:semver-major"]

  # ── npm: frontend ─────────────────────────────────────────────────────────
  - package-ecosystem: "npm"
    directory: "/frontend"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5
    groups:
      frontend-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      # Next.js major upgrades involve breaking changes and require manual migration
      - dependency-name: "next"
        update-types: ["version-update:semver-major"]
      # React major upgrades affect the whole component tree
      - dependency-name: "react"
        update-types: ["version-update:semver-major"]
      - dependency-name: "react-dom"
        update-types: ["version-update:semver-major"]

  # ── cargo: smart contracts ────────────────────────────────────────────────
  - package-ecosystem: "cargo"
    directory: "/contracts"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5
    groups:
      contracts-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      # soroban-sdk major bumps require contract re-audit and redeployment
      - dependency-name: "soroban-sdk"
        update-types: ["version-update:semver-major"]
      - dependency-name: "soroban-token-sdk"
        update-types: ["version-update:semver-major"]

  # Cargo fuzz harness is excluded from the main contracts workspace and has
  # its own lockfile, so it needs a separate update entry.
  - package-ecosystem: "cargo"
    directory: "/contracts/fuzz"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 3
    groups:
      fuzz-minor-patch:
        patterns: ["*"]
        update-types: ["minor", "patch"]

  # ── GitHub Actions ────────────────────────────────────────────────────────
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5
    groups:
      actions-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  # npm: repository automation scripts
  - package-ecosystem: "npm"
    directory: "/scripts"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5
    groups:
      scripts-minor-patch:
        patterns: ["*"]
        update-types: ["minor", "patch"]
